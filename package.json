{
  "scripts": {
    "build": "esbuild src/client.tsx --bundle --outfile=dist/bundle.js --define:'process.env.NODE_ENV=\"production\"'",
    "start": "sucrase-node src/server.tsx",
    "lint": "tsc && prettier -c **/*.{json,tsx}"
  },
  "dependencies": {
    "express": "^4.17.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "esbuild": "^0.8.29",
    "prettier": "^2.2.1",
    "sucrase": "^3.17.0"
  }
}
